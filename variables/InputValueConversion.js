// The prompt will return the user's input. We can then create a variable

let userInput = prompt();

console.log(2023 - userInput);

// to work with int numbers

// let userEntry = parseInt(prompt());
// console.log(2000 + userEntry);
